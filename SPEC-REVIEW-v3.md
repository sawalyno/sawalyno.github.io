# Sawalyno コーポレートサイト 仕様書レビュー（v3）

レビュー日：2026-05-05
対象：`apps/sawalyno-corp/SPEC.md`（最終更新 2026-05-05、SPEC-REVIEW v2 反映済み）
過去：`SPEC-REVIEW.md`（v1, 25 件） → `SPEC-REVIEW-v2.md`（v2, 14 件）

---

## 総評

v2 レビュー 14 件すべて反映済み。特に効いている改善：

- **V2-01（ブート尺の算術曖昧性）** が §5.5 と §7.1.1 の両方で「単純加算しないこと」を明記し、合計 1100〜1400ms に内包する旨が読み手に必ず伝わる構造になった
- **V2-04（mailto 難読化と SR の両立）** が href 側のみエンティティ化＋表示プレーン＋`aria-label` 併記で、3 層の防御線が綺麗に整理されている
- **V2-05（JSON-LD コメント混入）** が `<!-- TODO -->` を `<script>` 外側、`sameAs: []` 空配列を内側、と仕様書段階で valid JSON を保証する書き方に直っている
- **V2-07（CAA 能動確認）** が `dig CAA sawalyno.jp +short` の具体コマンドまで降りていて手順として完結
- **§13 の 3 分割（v1 / v1 レビュー / v2 レビュー）** で、いつ何を確定したかが将来読み返しても自明
- **§13.4「オーナー一任で確定」** という新カテゴリが、グレーで残っていた判断を明示的に着地させている

仕様書としての完成度は **実装着手可能レベルに到達**。ここから先は重箱の隅レベルで、公開前必須は **0 件**、推奨改善も **任意** の領域に入る。

---

## レビュー表（v3）

| # | 区分 | 該当箇所 | 重要度 | 指摘内容 | 推奨対応 |
| --- | --- | --- | --- | --- | --- |
| V3-01 | 整合 | §4.1 ディレクトリ構成 | 中 | `SPEC-REVIEW.md` のみが書かれており、現実には `SPEC-REVIEW-v2.md` も既に存在し、本レビューで `SPEC-REVIEW-v3.md` も加わる。ディレクトリ構成と現実が即ズレる。 | `SPEC-REVIEW*.md` のようにグロブ表記にするか、「レビュー履歴は `SPEC-REVIEW-v{N}.md` で版管理」と注記。今後増えるたびに SPEC を編集しなくて済む。 |
| V3-02 | 整合 | §6.5 ロゴ利用マトリクス vs §6.2 | 低 | §6.2 で「表示サイズは 16/24/32/64px のいずれか」と確定。§6.5 では `favicon.svg` / `apple-touch-icon.png` のサイズが「各規定サイズ」とぼかされており、`apple-touch-icon` の 180px は 16/24/32/64 の整数倍ではない（180 = 8×22.5）。 | `apple-touch-icon`（180px、ピクセル粒度規定外）と `favicon`（32px、整数倍）は規定の例外であることを §6.2 か §6.5 に補足。または「8x8 を SVG として埋め込み 180×180 にスケール、PNG ラスタライズ時は最近傍補間で粒度維持」と実装注を入れる。 |
| V3-03 | A11y | §5.4 / §7.1.5 | 低 | フッター Start ボタンが「装飾、リンクなし」だが、見た目はクリック可能なボタンに見える。マウスユーザーが押して反応しないと混乱する可能性。 | (a) `cursor: default` を明示、(b) `title` 属性または `<span role="presentation">` で「Decorative」とほのめかす、(c) hover の青リング演出が「これは押しても何も起きない装飾」というメッセージに見えるよう、メイン CTA（View Apps ↓）と視覚的に明確に差をつける、のいずれかを §7.1.5 に追記。 |
| V3-04 | A11y / SEO | §9.1 `<html>` lang | 中 | メタタグ集に `<title>` `<meta name="description">` などはあるが、**`<html lang="...">` の言語指定**が明記されていない。サイト本文は日本語、ターミナル内は英語の混在型。 | §9.1 のサンプルに `<html lang="ja">` を含めるか、§9.0 / §10.1 のいずれかで「`<html lang="ja">`、ターミナルブロックには `<pre lang="en">` を当てる」と方針を明記。SR の発音切替と検索エンジンの言語判定の両方に効く。 |
| V3-05 | デザイン | §5.5 セクションフェード | 低 | 「初回表示時に 200〜300ms で `translateY(8px) → 0` + opacity フェード」とあるが、**トリガー方法（IntersectionObserver / 単純な on-load 一斉発火 / scroll 連動禁止のはずだが…）が未明示**。1ページ・4セクションだと、スクロール前に全部発火するか、入域時に発火するかで体験が違う。 | (a) `IntersectionObserver` で各セクション初回入域時に発火、(b) もしくは on-load で 4 つを 80ms ずつスタッガー、のどちらかを §5.5 で確定。「スクロール連動アニメは原則使わない」の例外として位置づける。 |
| V3-06 | 整合 | §13.4 / §5.7 | 低 | §13.4 で `auto-fit, minmax(280px, 1fr)` 確定とある。同時に §5.7 では「`~767px` でモバイル微調整（フォントサイズ・余白）を許容」と書かれており、**段組のブレークポイントは無いがメディアクエリ自体はある**。命名上の「ブレークポイントレス」と矛盾しない確認が必要。 | §5.7 に「ここでの『ブレークポイントレス』は **段組決定のみ** を指す。フォントサイズ・余白・システムバー省略表記には従来どおり `768px` 等のメディアクエリを使う」と一行補足。 |
| V3-07 | 性能 | §10.2 / §10.3 | 低 | LCP 目標 ≤ 2.5s（fast 3G）に対し、**ヒーローのターミナルウィンドウは LCP 候補になりやすい**。タイポ＋ブートアニメ開始までに JetBrains Mono のロードが間に合わないと FOUT で LCP がブレる。 | (a) JetBrains Mono だけ `<link rel="preload" as="font" crossorigin>` で優先取得、(b) もしくは「LCP は ターミナルウィンドウの**枠**（CSS 描画）であってフォントではない」よう設計し、フォント未到着時は黒地のみ表示でカクつかない、と明記。 |
| V3-08 | 運用 | §11.4 更新フロー | 低 | 「本文に新しい日本語を追加した時」のサブセット同期が手動運用と書かれているが、**忘れた場合に何が起きるか（=新規文字が tofu になる）**の説明がなく、運用者が重要度を見誤る恐れ。 | §11.4 に1行：「サブセット URL を同期し忘れると追加文字が `□`（豆腐）表示になる。本番デプロイ前に該当セクションを目視確認」。失敗時の見え方を予告して同期忘れの心理的圧をかける。 |
| V3-09 | デザイン | §5.4 / §5.5 | 低 | レトロ UI 要素として `_ □ ×` のタイトルバーは仕様化されているが、**カードを「閉じる」ような遊び心**（クリックで `box-shadow` が一瞬大きくなって戻る、など）の許容可否が不明。「装飾、クリック領域なし」と書かれているため不可解釈もできるが、`active:transform: scale(0.98)` 程度の押下フィードバックは XP/7 風として欲しいところ。 | §5.5 に1行：「`_ □ ×` は装飾のためクリック領域・キーボードフォーカス対象としない。`scale(0.98)` 等の押下フィードバックも与えない（押せると誤認させる）」と消極側で確定するか、明示的に許容するかをオーナー一任。 |
| V3-10 | SEO | §9.2 JSON-LD | 低 | `Organization` 型を選択しているが、**サイト主体は「個人クリエイター Sawalyno」で、法人は運営元**という §1.2 の方針と整合させるなら、`Person`（Sawalyno）と `Organization`（Sawada LLC）を **2 つ並列** または `Person.worksFor` で関連付けても良い。現状は法人のみで個人が `founder` として埋もれている。 | (a) 現状維持で OK（審査は法人実在性が主目的のため）、(b) より忠実にやるなら `@graph` で `Person`（id: `#sawalyno`）と `Organization`（id: `#sawada-llc`）を並べ、`Person.worksFor` でリンクする選択肢を §9.2 に併記。実装はオーナー判断。 |
| V3-11 | 法務 | §2.2 SPEC 内部メモ | 低 | 「Play Console / App Store の運営者情報フォームには所在地・連絡先を実値で登録」とあるが、**ユーザーから直接の問い合わせがメールで届いた際に「住所開示してほしい」と言われた場合の対応方針**が未定義。 | (a) 「個人事業主・小規模法人として、サイト経由の問い合わせには住所開示義務はない。ストア提出済み情報で十分」、(b) 「請求があれば個別にメール返信で開示」のどちらか方針を SPEC 内部メモに追記。法的義務はないが運用の心構えとして。 |

---

## 観点別サマリ

### v2 → v3 で確実に良くなった点
- §13 の 3 + 1 分割（v1 確定 / v1 レビュー対応 / v2 レビュー対応 / オーナー一任で確定）が**意思決定の履歴管理として完成形**。これは将来の他プロジェクト SPEC のテンプレにできる。
- §9.0 の検索ターゲット方針（JP 主・EN 副）が「英文 description にしない理由」を明文化していて、後で誰かが触る時に safe。
- §11.2 手順 5 の CAA が「確認 → 条件分岐 → 追加」と能動形で完結。具体コマンド付き。
- ピクセル粒度の整数倍規定（§6.2）と利用マトリクス（§6.5）の組み合わせで、ロゴの一貫性が仕様レベルで担保された。

### v3 で新たに浮上した論点（重要度・中 = 2 件のみ）
- **V3-01**：レビューファイルの版が増えるたびに §4.1 を編集する運用は腐るので、グロブ化または注記化を推奨。
- **V3-04**：`<html lang>` の指定が抜けているのは v1〜v3 を通して見落とされていた基本項目。SR・検索エンジン両方に効くため中重要度。

### 公開前 必須対応（重要度：高）
**なし**。仕様書は v3 時点で実装着手可能。

### 公開前 推奨（重要度：中）
- V3-01：§4.1 のレビュー履歴をグロブ化
- V3-04：`<html lang="ja">` を §9.1 に追加

---

## v3 を踏まえた次アクション

1. **V3-04 のみ即座に反映**（30 秒の編集）。`<html lang="ja">` は基本項目で抜けは惜しい。
2. **V3-01** はリポジトリ運用の整理として 1 行で済む（30 秒）。
3. 残り 9 件（V3-02, 03, 05〜11）はすべて重要度低。**実装後にデモを見ながら判断**で十分。SPEC レベルでは打ち止めにしてよい。

---

## 結論

v1（25 件）→ v2（14 件）→ v3（11 件、うち中 2・低 9）と指摘数も重要度も明確に減衰しており、**仕様書としての成熟は十分**。これ以上は実装に進んでから返ってくるフィードバックの方が価値が高い段階に入った。

V3-04（`html lang`）のみ反映して **5/6 の実装着手に進むことを推奨**します。

以上。
